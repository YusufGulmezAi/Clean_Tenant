@page "/settings/language"
@rendermode InteractiveServer
@inject NavigationManager Nav

@*
    v0.2.13 — Dil ayarları artık birleşik profil sayfasındadır (/profile → Dil & Tercihler).
    Bu rota geriye dönük uyumluluk için /profile'a yönlendirir.
*@

@code {
    protected override void OnInitialized() => Nav.NavigateTo("/profile", replace: true);
}
