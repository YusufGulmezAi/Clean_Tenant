@page "/tenant/roles/new"
@attribute [Microsoft.AspNetCore.Authorization.Authorize]
@inject NavigationManager Nav

@*
    v0.2.13.b — Rol oluşturma artık /access > "Roller & İzinler" sekmesindeki
    RoleDialog modalı ile yapılır. Bu eski route (/tenant/roles/new) /access'e
    yönlendirir.
*@

@code {
    protected override void OnInitialized()
    {
        Nav.NavigateTo("/access", forceLoad: false, replace: true);
    }
}
