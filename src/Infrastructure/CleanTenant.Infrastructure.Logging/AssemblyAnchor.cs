namespace CleanTenant.Infrastructure.Logging;

/// <summary>
/// <para>
/// <b>CleanTenant.Infrastructure.Logging</b> projesinin assembly çıpa
/// (anchor) tipidir.
/// </para>
/// <para>
/// <b>Sorumluluğu:</b> Loglama altyapısı. Serilog konfigürasyonu (Console,
/// File, PostgreSQL → Log DB sink), enricher'lar (UserId, TenantId,
/// CorrelationId, IpAddress, UserAgent), <c>CorrelationIdMiddleware</c>
/// ve PII redaction politikalarını barındırır.
/// </para>
/// <para>
/// <b>İçerik durumu:</b> Faz v0.1.7 alt fazında doldurulacaktır.
/// </para>
/// </summary>
internal static class AssemblyAnchor;
